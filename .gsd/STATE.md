# GSD State

**CURRENT STATE (2026-07-18) — supersedes the "uncommitted / AWAITING USER / v1.0.0 pending" notes in the older S13 entries below; those are historical.** All S13 model-evaluation work is COMMITTED and **v1.0.0 is tagged and pushed** to the public remote (christo0192/go-pilot). Shipped this arc: DeepSeek V4 Pro as default workhorse; **Kimi K2.5** for doc-QA (3-trial confirmed 5/5) and extraction (kept per user despite the extract-03 strict-schema regression); K2.6 retired from active routing (alias kept for reproducibility); K3 evaluated + rejected (~$15/M). Production routing lives in `config/router.json` `ikey-prod` (ikey-hybrid frozen as the benchmark baseline). Hybrid arm-D experiment formally DROPPED (D38). CI clean-machine installer-doctor extended to macOS (green). CHANGELOG/package.json at 1.0.0. Suite 399/399, CI green.
**PILOT HARDENING MERGED (2026-07-18, PR #1 → main `13092ff`).** The runtime cutover (old open #1) SHIPPED: `config/runtime.json` makes `ikey-prod` the default profile with deterministic precedence (CLI > GOPILOT_PROFILE > deploy/.env > runtime default, `src/config/profile.mjs`); K2.5→DeepSeek fallback wired on extract+doc-qa (validated: schema/citation, all attempts accounted, fails closed); `kimi` alias repointed to K2.5 across pi-worker.sh/gateway-call.mjs/CLAUDE.md/risk.mjs (`kimi26` kept for reproducibility); CI-gated `check:routing`+`check:metrics` (fixed to be clean-checkout + Windows-safe); dispatch/validation/CLI hardened; key fingerprint scrubbed. Reviewed + verified by Claude before merge (415 unit/28 integ/3 live, CI green ubuntu/macos/windows). Model evidence UNCHANGED and not overclaimed: doc-QA 97.1, extraction 89.4 (deliberate tradeoff, not proven ≥90), composite 93.3, 100% reliability/36 runs, K2.5 ~91% more tokens than DeepSeek. Docs: docs/HANDOVER-2026-07-18-PILOT-HARDENING.md, production-routing.md, production-metrics.md.
**PROD-GATE CAMPAIGN RUN (2026-07-18): extraction CLEARS ≥90 via the production path. 6/7 pre-registered gates pass.** Ran the pre-registered gate: 6 extraction fixtures × 3 trials through `runTask` on `ikey-prod` (K2.5 primary + per-fixture JSON schema validation + DeepSeek fallback, no --repair), graded blind. Setup: authored + gold-validated JSON schemas for the 5 JSON-extraction fixtures (extract-04 is text-summary, schema-less); fixed a REAL latent bug the hardening pass left — the benchmark dispatcher (ikey-dispatch.mjs) still expected an alias but run.mjs now passes the pinned version → added `modelAlias`-preferring resolution; added fallback-rate capture to campaign arm-A records. RESULTS (docs/live-test-results-v3-prodgate.md): **extraction 90.0 (was 89.4, DeepSeek 87.2, +2.8) ✅**; reliability 100% (0/18) ✅; cost/success $0.0077 ✅; latency 150s ✅; **the extract-03 strict-schema regression is GONE (73.3→76.7, now ties DeepSeek)** — schema-guided prompting fixed it. FALLBACK fired just 1/18 (5.6%, extract-medium-05:t3 K2.5→DeepSeek, DeepSeek won) → the lift is from SCHEMA-GUIDED PROMPTING, not the fallback. One gate MISS: extract-04 −3.3 vs DeepSeek — but that's the free-text SUMMARY fixture (no schema, outside the machinery), single-trial noise. Honest caveat: quality lift over prior K2.5 is marginal (+0.6 area mean); fallback under-exercised at 5.6%. Uncommitted (→ PR): 5 fixture schemas, ikey-dispatch bug fix, campaign.mjs fallback capture, prodgate doc.
**THREE REMAINING ITEMS CLOSED (2026-07-18):** (1) **LIVE Pi-agentic dispatch VALIDATED** — running it found a REAL bug: `pi-gopilot.sh` was a stale placeholder forcing `--provider openai` on gateway ids (400s); it never worked live. Fixed to use the `ikey/` provider prefix like the working `pi-worker.sh`; a live `bin/gopilot.mjs run` now dispatches through the real Pi agent → ikey gateway → correct DeepSeek output (validated:true). (2) **install.ps1 `-Doctor` ADDED** (verify-only parity with install.sh --doctor) + a `windows-latest` CI installer-doctor job — closes clean-Windows install verify (S06/6.5); PowerShell validated by CI (no local pwsh). (3) **Multi-agent sign-off ASSESSED** → all 5 classes revert-to-single (D39): the framework's multi-pane modes don't claim token reduction (candidate-race is cost-opt-in D37; decomposition adds overhead), so the gate can only revert — the active default. Doc: docs/multi-agent-signoff-status.md.
**ONE-CLICK SETUP BUILT (2026-07-19, PR #6):** a first-timer's fresh-laptop transcript ("go pilot complicated") exposed the real install experience: exec bits never committed (Permission denied everywhere), pi/herdr behind --full (herdr never installed), docker-group/session hell, sudo prompts, key pasted into the wrong .env field. ALL root-caused + fixed: scripts committed 755; install.sh --one-click (implies --full, installs herdr, persists PATH, injects $GOPILOT_WORKHORSE_KEY into the CORRECT field fill-empty-only, sudo-fallback compose). NEW entry points: setup.cmd (Windows double-click; root work via passwordless `wsl -u root` in scripts/oneclick-root.sh incl. Pi-as-root for the EACCES npm prefix; wsl --shutdown restart for group+systemd; clone ~/Go-pilot; ends opening a herdr terminal via scripts/oneclick-launch.sh; key flows via WSLENV, never a Windows file; CRLF pinned in .gitattributes) + setup-macos.command (Finder double-click; brew NONINTERACTIVE, Docker Desktop launch+wait; window becomes the herdr terminal). Hermetic tests for key-injection + PATH idempotency; doctor still exit-0; suite 417/417; CI green. ACCEPTANCE PENDING: the user's planned fresh delete-and-reinstall on the other Windows laptop is the real end-to-end test — not verifiable from this machine.

**LIVE TOKEN-RECOVERY FIXED (2026-07-18):** root cause was a snippet mismatch — `dispatch.mjs` builds the match-snippet from the composed prompt (newlines→spaces via sanitize), but Pi stores the prompt in its session JSONL with newlines JSON-ESCAPED (`\n`), which sanitized to the letter "n"; the coordinator's prompt starts `## policy\n[YAGNI]…` so a newline landed in the first 60 chars → snippets never matched → silent null. Fix: `pi-usage.mjs` sanitize now collapses escaped `\n`/`\r`/`\t` to spaces first. Refactored pi-usage.mjs to export `sanitize`/`recoverUsage` (+ `GOPILOT_SESSIONS_ROOT` override) with a regression test (fixture with an escaped-newline prompt). VALIDATED LIVE: `bin/gopilot.mjs run` now reports real tokens (total 11,285) + cost ($0.0093), was None. Suite 417/417. (Cost is `estimated:true` from calibrated rates × recovered tokens.)

---

**S13 EXTRACTION confirmation (2026-07-18): DO NOT blanket-promote to K2.5 — 4/6 gates.** 12 more arm-A trials on the 6 extraction fixtures (3 trials total → out-v3-k25), graded clean, 0 failures, max 166s. eval-area.mjs (generalized eval-docqa, verified reproduces doc-QA 6/6). Extraction K2.5 vs DeepSeek 3-trial medians: extract-01 +6.7, extract-02 +0 (tie 100), extract-03 **−3.3**, extract-04 +0 (tie 96.7), extract-hard-05 +3.3, extract-medium-05 +6.7. Gates (floor 90, clear-win +2): area-mean +2.2 ✅ (clear avg win), reliability 100% ✅, cost/success **$0.007** ✅, latency 166s ✅ — BUT trial-median 89.4 <90 ❌ and 1 HARD regression (extract-03) ❌. K2.5 wins reasoning-style extraction (+3-7) but LOSES strict-JSON-schema extraction (extract-03: meeting-notes→exact-keys/ISO-dates, where DeepSeek's determinism wins). NOT uniformly better. **USER DECISION (2026-07-18): KEEP extract→kimi-k2.5-ikey in ikey-prod as-is** — deliberate, betting the +2.2 avg win + low cost ($0.007) + 100% reliability outweigh the single strict-schema regression (extract-03). Known accepted tradeoff: strict-JSON-schema extractions may score ~3pt below DeepSeek; a future K2.5-candidate+schema-validation+DeepSeek-fallback wrapper (arm-D pattern) could recover that if it becomes worth it. Result doc: docs/live-test-results-v3-k25-extraction.md. eval-area.mjs generalizes eval-docqa (removed); reproduces doc-QA 6/6.

**S13 DOC-QA PROMOTED to K2.5 + `ikey-prod` profile created (2026-07-18): 5/5 gates pass.** Doc-QA confirmation (2 more arm-A trials on the 6 doc-qa fixtures = 12 calls → out-v3-k25, 3 trials total, graded clean): trial-median doc-QA quality **97.1 (≥97 ✅)**, reliability **100%** (0/18 fail ✅), **every fixture ≥ DeepSeek** (docqa-01 +2.5, 02 +0, 03 +2.5, 04 +0, hard-05 **+7.5**, medium-05 +2.5 — 0 regressions ✅), cost/success **$0.0125 (<$0.02 ✅)**, max latency **143s (<240s SLA ✅)**. Evaluator: eval-docqa.mjs. APPLIED user's target routing as NEW `ikey-prod` profile in config/router.json (ikey-hybrid left FROZEN as benchmark baseline; K2.6 kimi-ikey alias kept in models.json for reproducibility but REMOVED from all active routing): doc-qa→kimi-k2.5-ikey, extract→kimi-k2.5-ikey (validated-candidate), analyze(→analysis+spreadsheet)→deepseek-ikey, draft(creative)→deepseek-ikey, summarize→deepseek-ikey, all else→deepseek-ikey; high-risk/low-confidence→opus via __judgment__ default. All refs resolve; unit suite 399/399. No live default-profile switch exists (profile passed at runtime), so NO automatic production cutover happened — ikey-prod is defined, ready to select. NEXT/AWAITING USER: wire ikey-prod as the runtime/orchestrator default if desired; commit the large uncommitted set (config/{models,router}.json, calibration.json, deploy/pi-models.ikey.json, gateway-call.mjs, ikey-dispatch.mjs, campaign.mjs, grader.mjs, grade-campaign.mjs, compare-{hybrid,k25}.mjs, eval-docqa.mjs, probe-k3.mjs, docs) after code-review.

**S13 KIMI-K2.5 screening DONE (2026-07-18): ties DeepSeek overall, WINS doc-QA+extraction, loses creative; don't blanket-promote but selective-route candidate + strictly replaces K2.6.** A separately provisioned gateway credential unlocked test/kimi-k2.5. Wired kimi-k2.5-ikey + ikey-k25 profile (kimi-ikey untouched); accepts temp 0 (no coercion, unlike K3); calibrated $2.835/M settled (cheaper than K2.6 $3.847). Bumped dispatcher default timeout 200→300s (K2.5/K3 hit 180-200s on hard fixtures). Ran 21 fixtures × 1 trial arms A+C → out-v3-k25, graded clean (63 records, 0 grade-errors). RESULTS (compare-k25.mjs): mean quality **K2.5 90.0 = DeepSeek 90.0 >> K2.6 81.9**; K2.5 tokens 77,992 (60% > DeepSeek 48,981, ~=K2.6); cost/success $0.0105 (< lean-Opus $0.0438, > DeepSeek $0.002); arm-A failures 0. Per-area K2.5 vs DeepSeek: **doc-QA +4.2 (98.8, near Opus 99.6), extraction +2.8, analysis +0.6** but **creative −17.5 (68.8; 1-trial 75 & 62.5, high variance), spreadsheet −3.3**. Screening gates 2/5 pass (fails ≥93 quality, fails clear-narrative-win). VERDICT: not a blanket workhorse (ties DeepSeek at 5× cost, 60% more tokens); selective doc-QA/extraction routing candidate; strictly dominates K2.6 (better+cheaper+reliable).

**S13 KIMI-K3 wired + probed (2026-07-18): access OK but ECONOMICS likely disqualify K3 as a workhorse.** Did a+b without touching kimi-ikey: added `kimi-k3-ikey`→`test/kimi-k3` to config/models.json, cloned `ikey-hybrid`→`ikey-k3` profile (only kimi-ikey→kimi-k3-ikey), added test/kimi-k3 to deploy/pi-models.ikey.json, `kimi3` raw alias in gateway-call.mjs, and dispatcher coercion `temperature=1` for kimi-k3 (gateway 400s on temp≠1 — verified). All router profiles resolve; config doctor clean. 3-probe test (analysis-02, docqa-hard-05, data-hard-01): ✅ model=test/kimi-k3, reasoning counted, no empty, cap honored, temp-coercion works on real fixture temps (0.3/0.1→1). ⚠️ FINDINGS: (1) **K3 unmetered on Ikey TEST gateway** — 2 probes ~6000 out-tok moved settled spend by exactly $0; pinned calibration.json at PUBLISHED $15/M output (conservative blended upper bound) so cost reflects reality not free tier. (2) **K3 real cost ≈ or ABOVE lean-Opus**: ~3500-tok easy call ≈$0.05, data-hard-01 8756 tok (7024 reasoning!) ≈$0.12 — vs lean-Opus $0.0438/success, DeepSeek $0.002, K2.6 $0.0308. K3 is ~4× K2.6, ~18× DeepSeek. (3) **Very slow**: data-hard-01 took 236s (>200s dispatcher default → first probe aborted; would need timeout bump for 21-fixture run). AWAITING USER before the 21-fixture run: K3's cost/latency likely fail their own cost gate — proceed for quality-only signal (gateway free now) or stop. Probe script: scripts/baseline-rig/probe-k3.mjs.

**S13 HYBRID experiment RUNNING (2026-07-17): DeepSeek→Kimi synthesis, arm D, per Codex 3-refinement design.** Built the two-stage hybrid arm in campaign.mjs: (1) DeepSeek candidate [fallback+paired baseline], (2) DeepSeek evidence pack, (3) Kimi synthesis from pack ONLY. VALIDATE synth (non-empty, not truncated, numPreserved≥0.8 of pack numbers) → use synth; else FALL BACK to DeepSeek candidate (Kimi tokens still counted; a Kimi failure is never an unresolved campaign failure → hybrid reliability floor = DeepSeek's). Emits companion `Dcand` record (pre-synth candidate) so paired HYB−CAND delta isolates Kimi's contribution. numTokens/numPreservation helpers + pack-vs-source grounding diagnostic. New helpers unit-tested; full suite 399/399. Smoke verified: extract-02 synth dropped ⅓ of pack numbers (numPres 0.67) → fell back, ZERO failures. Campaign LIVE: `bjwweu19e`, 63 D-runs (21 fixtures × 3 trials) → `out-v3-hybrid`, seed 20260713, --repair. SLOW (~1-2min/run: 3 calls + slow Kimi). Token-heavy (analysis-01 17.7k w/ repair) — the "tokens<Kimi-only" gate will be tight. Comparison+gates script READY: `scripts/baseline-rig/compare-hybrid.mjs` (reads out-v3-hybrid D/Dcand + out-v3-deepseek A/B + out-v3-trim Kimi-A; emits `docs/live-test-results-v3-hybrid.md` with Report-1 all-task ablation + Report-2 selective narrative-only policy + 7 promotion gates). NEXT: campaign done → `CAMPAIGN_OUT=out-v3-hybrid node grade-campaign.mjs` (grades D+Dcand, 126 Opus judge calls — usage-limit risk, resumable) → `node compare-hybrid.mjs` → deliver both reports + gate verdict. WAIT for user. Uncommitted: campaign.mjs (arm D), grader.mjs, grade-campaign.mjs, campaign.test.mjs, compare-hybrid.mjs, config/router.json, docs reports.

**S13 DeepSeek-only experiment DECISION (2026-07-17): DON'T promote as SOLE workhorse; DO make DeepSeek the default over Kimi.** 21 previously-Kimi fixtures × 3 trials × arms A+C on `deepseek-ikey` (new `ikey-deepseek` profile); frozen trim Opus arm-B reused (zero new Opus arm spend); $0.233 gateway settled. Report: `docs/live-test-results-v3-deepseek.md`. Harness fixed first (all 3 defects the user flagged): Arm-A now honors fixture max_tokens (killed silent 8k cap), failed-repair usage counted (mergeUsage always + errored-repair projected cost), thinking-disable PROBED not patched (gateway drops all 4 params — DeepSeek reasoning cannot be disabled; ran thinking-ON per user). Also fixed a grader bug: unparseable/truncated judge replies were scored 0 instead of flagged grade-error (10 creative/analysis records were false-zeros — corrected; parser now recovers the scores block from truncated replies; grader.test 15/15). **User's 6 promotion gates: 4 PASS / 2 FAIL.** PASS: zero empty/truncated (63/63), spreadsheet 91.7 (≥90), tokens −40.2% vs Kimi (48,981 vs 81,940 median-tok), cost/success $0.0020 = 22× under lean-Opus $0.0438. FAIL (binding): completed quality 90.1 (<95), reliability-adjusted 90.1 (<94–96). DeepSeek is clearly BETTER than Kimi (reliability-adj quality 90.0 vs 81.9; fixed Kimi's 2 catastrophic zeros — docqa-01 0→90, data-medium-01 0→100; 40% fewer tokens) but plateaus ~90 completed quality, below the sole-workhorse bar. Per-area §2 gates still keep-on-Opus for quality (all areas <95% of Opus). Uncommitted code (needs review before commit): campaign.mjs, grader.mjs, grade-campaign.mjs, config/router.json. AWAITING USER: adopt DeepSeek as default workhorse (swap ikey-hybrid Kimi→DeepSeek) / keep hybrid / iterate to lift quality / commit the harness+grader fixes.

**S13 P4 trim GATE DECISION (2026-07-15): NO-GO on full v3.** Trim campaign complete: 114/114 runs, 114/114 graded (5 arm-B extraction grade-errors from a transient `claude exited 1` were re-graded this session — backup `graded-runs.jsonl.bak-gradeerror`). Report: `docs/live-test-results-v3-trim.md`. **All 5 §11 gates FAIL**: A quality-when-completed 94.9 (<98) · reliability-adjusted 87.4 (<96; B2=95.6) · cost/success $0.0097 = 31.5% of B2 $0.0308 (needs ≤20%) · quality/1k-tok 0.78 < C 0.90 · A had 2 empties + 1 truncated. Per-area: WIN math + multi-step-reasoning; coding/repo at quality parity but token-heavier than C (scaffolding overhead on small tasks); doc-qa 80.8 / spreadsheet 58.3 / analysis 89.4 / creative 85.0 all below the 95%-of-B floor. Headline economics still strong: A is 98.4% cheaper than B1 (CI [0.010,0.022]), quality ratio A/B 0.914. Conditional full v3 (5 arms, 3 trials) NOT triggered; v1.0.0 tag still held. AWAITING USER: accept trim verdict as the documented result / iterate on the weak areas (routing or repair for doc-qa+spreadsheet+A-reliability) before re-running / tag anyway with honest caveats.

**S13 path-to-9 execution (2026-07-13, autonomous)**: P1✅ (CI green: unit matrix ubuntu/macos/windows + integration + zero-deps gate + shellcheck + gitleaks; GitHub secret-scanning/push-protection/branch-protection; SECURITY.md; src/validation + src/router/risk classifiers wired into CLAUDE.md; key-echo test; rotation SKIPPED per user — keys kept for testing). P2✅ (breaker/budget/journal in pi-delegate; agentic token accounting via Pi session logs — usage no longer null; --sandbox worktree isolation live-verified; 8 fault-injection tests). P3✅ (5 zero-dep modules: templates ≤10% scaffold, extraction w/ evidence verification, spreadsheet preprocessor, evidence packs, session compression). P4 build✅ (Opus-only headline judge + adjudication queue; failed attempts cost money; v3 reliability-adjusted metrics + §11 gates; B2 lean-Opus DERIVED analytically — measured CLI tax 50,050 cacheWrite + 15,166 cacheRead ≈ $0.508/call, calibration.json). P4 trim RUNNING (114 runs = 38 fixtures × A/B/C × 1 trial, --repair, seed 20260713, CAMPAIGN_OUT=scripts/baseline-rig/out-v3-trim, checkpointed — resume = rerun same command). P5 mostly✅ (gopilot-status.mjs live; install.sh --doctor 12/13 + CI clean-machine job; Pi pinned 0.80.6; CHANGELOG + RUNBOOK; v1.0.0 tag pending benchmark). Windows CI flake fixed (workspace test made hermetic). NEXT: trim completes → grade (Opus-only) → aggregate → §11 gate decision → conditional full v3 (5 arms incl. D multi-turn, +fixtures, 3 trials) → results doc → tag v1.0.0 → report to user and WAIT.

**S12 PUBLISHED to GitHub (2026-07-13)**: remote = https://github.com/christo0192/go-pilot (PUBLIC, default branch `main`, local branch renamed master→main). Full history REWRITTEN via git-filter-repo: all 59 commits re-identified as Chris <christo.b@interviewkickstart.com>, india.sales scrubbed from every blob; pre-rewrite backup bundle in the session job tmp dir (ephemeral). Secret audit of all history: clean (only fake test keys + placeholders). Plug-and-play: `install.sh ensure_orchestrator()` (pi-delegate shim, Pi provider templated via __GOPILOT_REPO__, global skill from deploy/global-skill.gopilot-orchestrate.md), `WORKHORSE_GATEWAY_KEY` added to deploy/.env.example (the one key a user fills), README quickstart = clone → install → key → claude. NOTE: repo commits now use christo.b identity (git config local).

**S11 orchestrator framework LIVE (2026-07-13)**: production daily-driver flow shipped — repo-root `CLAUDE.md` (Claude Code = Opus orchestrator: risk-classify → route → delegate → verify → repair → assemble; hybrid writing policy w/ `workhorse-only` opt-out; billing invariants) + `.claude/skills/{orchestrate,pi-workers,herdr-panes}` + hardened `scripts/pi-delegate.sh` (flags `--raw/--repair/--class/--timeout/--max-tokens`, JSONL metrics ledger → `scripts/baseline-rig/out/delegate-log.jsonl`, exit codes 0/2/3/4/5, mechanical repair strict→sibling) + `scripts/gateway-call.mjs` (non-agentic worker, exact usage incl. reasoning tokens). All live-tested (raw both models, agentic pane w/ zero leak, forced repair ladder). DEFERRED by user decision: benchmark v3 rerun, extraction/spreadsheet/retrieval pipelines, multi-turn compression (Codex upgrade plan §3-5,§7,§10-11 — planned, not built).

**Phase**: executing
**Active Milestone**: M001 — Go-pilot build (PLAN.md sprints 0–7)
**Active Slice**: S10 — Live-runtime foundation (Codex-built, then reviewed+fixed+committed). Integrates dispatch adapters, retrieval, prompt/cache builder, rules discovery, workspace checkpoints, execution contracts into run.mjs.
**Active Task**: — S10 landed. Remaining Sprint 8 work still user/infra/machine-gated: 8.8 (Ikey key+credits — needed to actually RUN the new live adapters), 8.9 (fixtures+quota), 8.14 (Mem0 services+vuln scan+backup), 8.15 (clean boxes), 8.16 (pilot).
**Progress**: S00–S07✅ · S08 Phase A 8.1–8.7✅ · Phase B 8.10/8.11/8.12/8.13✅ · S10 live-runtime foundation✅ (dispatch/context/prompts/instructions/runtime/benchmarks modules + rebuilt run.mjs) · 310/310 (unit 287 + integration 20 + live 3), zero deps · HEAD (S10 commit)
**S10 review (2026-07-12)**: Codex built the foundation but 2 tests failed (rg is a shell function → spawnSync ENOENT → silent-empty retrieval). Fixed: retrieval degrade-safe (node-fs fallback) + re-hermeticized lifecycle test. Security Engineer + Code Reviewer passes → fixed HIGH candidate-race Promise.all→allSettled, output-cap DoS, `--`/model-validation flag-injection, secret redaction in errors, breaker isolation, durability reconcile, captureWorkspace opt-out. Committed `e698961`.
**D37 candidate-race governance (2026-07-12, user decision)**: candidate-race is NOT gated by the efficiency sign-off (it makes no efficiency claim — it trades tokens for reliability). Instead `contracts.mjs` codifies the mode taxonomy (`modeGovernance`: efficiencyGated vs costOptIn) and cost-opt-in modes require explicit `allowParallelCost` (CLI `--allow-parallel-cost`) on a LIVE run + optional `contract.maxCostUsd` cap. Fixed `downgraded` mislabel (now `execution !== requestedMode`). 313/313.
**Workhorse gateway = Ikey (D34)**: user's own hosted LiteLLM (https://ikey-gateway.fly.dev), one key, no OpenRouter markup. Live worker (8.8) needs the Ikey key + credits topped up (Ikey-side). Local LiteLLM now behind a compose profile.
**Remaining**: Phase A 8.6/8.7 (buildable now) · 8.8 live workhorse (Ikey key+credits) · 8.9 benchmark (fixtures+quota) · Phase B 8.10–8.16 deferred · SECURITY: rotate exposed OpenAI key.
**Test buckets (8.3)**: `npm run test:unit` (hermetic, no ports/tools) · `test:integration` (loopback fakes: mesh, mem0-client) · `test:live` (needs real cce/rtk/Mem0, self-skips). Bucketer = `scripts/run-tests.mjs`, filename-suffix convention `.integration.test.mjs`/`.live.test.mjs`.
**SCOPE (D31)**: hybrid is first-class — rig must serve anthropic-only / codex-only / hybrid / open-first users. S02 builds the workhorse plane (also fixes the per-worker 44k Claude Code overhead — Pi/open-model panes carry none).
**Installed this session**: Docker (native WSL2), Pi 0.80.6, rtk 0.43.0, CCE 0.4.25, uv. Mem0 LIVE. NEEDS: ≥1 open-model key (OpenRouter recommended) for LiteLLM.
**Model Profile**: pure-anthropic (claude+codex installed; no LiteLLM/Pi needed yet)
**Last Updated**: 2026-07-11

## S03 — Router + Context Tiering (CORE COMPLETE ✅ 2026-07-09)
- 6/6 tasks built in `src/` (was empty): T01 router, T02 TOON (42%<JSON), T03 boundary guard, T04 overhead
  report, T05 YAGNI fragment, T06 agent-comms mesh. All Node ESM zero-dep (D20); `node --test` 42/42, clean exit.
- See S03-SUMMARY.md. Decisions D20–D22, KNOWLEDGE updated (node:test gate; net.Server closeAllConnections gotcha).
- Deferred pilots (Steps 3.4 rtk / 3.5 CCE / 3.6 context-mode) — tools NOT installed (D21). T03 guard already
  provides the Reference/Compressed seam they'd plug into. Revisit after tool install (user/infra decision).

## S04 — Memory PURE-CODE SEAMS COMPLETE ✅ 2026-07-09
- 5/5 in src/memory/: store (Tier-1 atomic claim+boomerang), gate (validate-before-compress), mem0-adapter
  (mock {add,search}), promotion (keepers-only), recall (bounded session-start injection). See S04-SUMMARY.md.
  Pipeline: store→gate→promotion→adapter→recall. Decisions D23. suite 80/80, zero deps, 28 serial runs 0 flakes.

## S07 — Instrumentation + Acceptance COMPLETE ✅ 2026-07-09
- src/metrics/: metrics (per-run record + computeRun), acceptance (evaluate vs #10 targets ≥20%/≤5%),
  signoff (per-class GO vs revert; no-data→revert, D17 safe default). 115/115, zero deps. See S07-SUMMARY.md.
- Residual: LIVE per-class sign-off needs real baseline-rig runs fed into signoff() (D17).

## S06 — Self-installing repo ✅ 2026-07-09
- install.sh (mac/WSL) LIVE-verified idempotent no-op (exit 0, Mem0 200) — Step 6.1 done-when met. install.ps1
  authored (Windows parity; live run = 6.5). docs/INSTALL.md both OSes + revert. 6.3 compose ✅, 6.4 templating ✅.
- Remaining S06: only 6.5 fresh-machine Win+Mac acceptance (needs clean boxes + teammate). Decisions D27.

## M001 near-complete (94%) — final 2 items are USER-GATED (2026-07-10)
- **Live per-class sign-off (D17)**: harness READY (scripts/baseline-rig/run.py, herdr 0.7.3 running,
  metrics/quality-rubric.md, src/metrics/signoff.mjs). BUT needs: real per-class task fixtures (only
  tasks/trivial-smoke.json exists), real Claude/Codex runs (Max quota), and subjective quality scoring. This is
  exactly the T04 the user DEFERRED (D17). To finish: add fixtures for the 5 classes → run run.py single vs
  multi → score per rubric → feed records to signoff(). User decision (quota + fixtures + judging).
- **S06/6.5 fresh-machine verify**: run install.ps1 on a clean Windows box + install.sh on a clean mac (teammate
  acceptance). Can't be done from this WSL box.
- Everything else (S00–S07 core, real Mem0 live, Pi skills, installers, rtk/CCE) is DONE + committed.
- SECURITY still open: rotate the OpenAI key pasted in chat (gitignored deploy/.env; safe from git).

## S04b Tier-2 wiring COMPLETE ✅ 2026-07-09 (the "no-install win" — now done)
- src/memory/tier2.mjs createTier2Adapter({mode:auto|mock|mem0}); promote/recall made async → work with the
  async real mem0-client AND sync mock. Live integration test proves gate→promote→mem0→recall. Memory = DONE.
  Decisions D28. Suite 135/135. To use real Mem0 in orchestrator code: createTier2Adapter({mode:'mem0'}).

## REASSESS (2026-07-09, post-Tier2-wiring): remaining M001 is ALL install-gated / other-machine
- S05 (Pi workflow skills brainstorm→…→auto + Phase-0 gate) — needs Pi (`npm i -g @earendil-works/pi-coding-agent`).
- S03 pilots (3.4 rtk / 3.5 CCE / 3.6 context-mode) — need rtk + CCE installed.
- Live per-class sign-off (D17) — needs real baseline-rig runs to feed src/metrics/signoff.mjs.
- S06/6.5 fresh-machine Win+Mac verify — needs clean boxes + a teammate.
- No buildable-now-without-install work remains. Next session: pick an install path with the user.

## (earlier reassess note, superseded)
Pure-code buildable-now work is EXHAUSTED. Remaining:
- S05 (Pi workflow skills brainstorm→…→auto + Phase-0 gate) — needs Pi installed (`npm i -g @earendil-works/pi-coding-agent`).
- S06 (self-installing repo install.sh/.ps1 + docker compose) — needs the deferred services to exist to be meaningful.
- Env-deferred: S03 pilots (rtk/CCE), S04 real Mem0 (Docker), S07 live per-class sign-off (baseline runs).
NEXT-SESSION OPTIONS (all need a user install decision): (a) install Docker → wire real Mem0 behind D23 +
run live acceptance/sign-off; (b) install Pi → S05 skills; (c) install rtk/CCE → S03 pilots; (d) start S06
install scripts (can scaffold idempotent installers now, but end-to-end verify needs the services).

## S00 — CLOSED ✅ (GO to build)
- T01 scaffold ✅ · T02 concurrency GO (10 sessions) ✅ · T03 baseline rig ✅ · T04 SKIPPED (D17 policy)
- Key: D16 lean-worker cuts ~60% cost/call. Rig ready for per-class validation on demand.

## S01 — CLOSED ✅ (substrate proven)
- herdr 0.7.3: headless socket API + `pane run → wait output → pane read` dispatch (D18). claude+codex lean
  workers, worktree-per-pane (D19), advisory locks. See panes/herdr-orchestration.md. Server runs bg.

## Installed: git, node v22.23, npm, python3 3.14 (no pip), claude 2.1.204, codex 0.143.0. Missing: wezterm, pi, rtk, docker.
